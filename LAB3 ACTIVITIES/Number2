    // LabAct3.2
    public static String asteris(String s) {
       if (s.length() <= 1) {
            return s;
        } else {
            return s.charAt(0) + "" + asteris(s.substring(1));
        }

    }
